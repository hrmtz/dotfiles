# ~/.zsh_secrets — machine-local secrets (NOT tracked by git)
# Copy this file to ~/.zsh_secrets and fill in your values:
#   cp ~/project/dotfiles/zsh/.zsh_secrets.template ~/.zsh_secrets
#   chmod 600 ~/.zsh_secrets

# 1Password Service Account
export OP_SERVICE_ACCOUNT_TOKEN=""
